# Scripts

Purpose: future deployment helper scripts.

Expected responsibilities:

- Validate environment variables before deploys.
- Run smoke checks against the public frontend.
- Avoid printing secrets or raw database URLs.
- Never open PostgreSQL to public networks.

No deployment scripts are active yet.
