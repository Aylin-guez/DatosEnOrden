# Docker

Purpose: future container packaging notes for DatosEnOrden services.

Expected responsibilities:

- Package the Reflex frontend service.
- Keep service environment variables outside source control.
- Use read-only database credentials for public services.
- Keep ingestion/write jobs separate from public read containers.

No Docker image or compose file in this folder is active yet.
